This directory is intentionally reserved for future client hooks.

Current architecture defaults to React Server Components and Server Actions.
Add hooks only when a clear client-side requirement exists.
